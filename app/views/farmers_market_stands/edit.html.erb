<%= form_with url: "/farmers_market_stands/#{@farmers_market_stand.id}", method: :patch, local: true do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.submit 'Submit' %>
<% end %>
