<a href=/produce_items>Produce Items</a>
<a href=/farmers_market_stands>Farmers Market Stands</a>
<h1>Produce Items</h1>
<% @produce_items.each do |item| %>
  <%= item.farmers_market_stand_id %>
  <%= item.created_at %>
  <%= item.updated_at %>
  <%= item.type %>
  <%= item.weight %>
  <%= item.price_by_weight %>
  <%= item.organic %>
<% end %>
